meta {
  name: Send otp
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/otp
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{demoEmail}}",
    "type": "EMAIL_VERIFICATION"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
