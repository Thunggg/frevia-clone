meta {
  name: Mark Conversation As Read
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/conversations/2/read
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}
