meta {
  name: Like / Unlike Comment
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/api/forums/posts/:postId/comments/:commentId/like
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  postId: 
  commentId: 
}

settings {
  encodeUrl: true
  timeout: 0
}
