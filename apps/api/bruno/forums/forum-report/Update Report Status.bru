meta {
  name: Update Report Status
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/forums/reports/2/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "RESOLVED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
