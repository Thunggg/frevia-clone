meta {
  name: Delete Job
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/manage-jobs/5
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status should be 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Success should be true", function () {
    expect(res.getBody().success).to.equal(true);
  });
}
