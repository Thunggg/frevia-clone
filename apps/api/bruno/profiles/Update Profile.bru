meta {
  name: Update Profile
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/profiles/1
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "displayName": "Sarah Jenkins",
    "title": "Senior UI/UX Designer",
    "bio": "I am a passionate UI/UX designer with over 8 years of experience creating user-centric digital products.",
    "availabilityStatus": "AVAILABLE"
  }
}
