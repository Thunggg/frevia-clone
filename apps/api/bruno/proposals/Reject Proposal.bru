meta {
  name: Reject Proposal
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/proposals/{{proposalId}}/reject
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  proposalId: 1
}
