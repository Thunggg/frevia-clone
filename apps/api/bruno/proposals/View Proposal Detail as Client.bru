meta {
  name: View Proposal Detail as Client
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/proposals/{{proposalId}}/client
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  proposalId: 1
}
