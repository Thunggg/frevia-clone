meta {
  name: View Proposal Detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/proposals/{{proposalId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  proposalId: 1
}
