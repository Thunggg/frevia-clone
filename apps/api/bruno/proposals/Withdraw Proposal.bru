meta {
  name: Withdraw Proposal
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/proposals/{{proposalId}}/withdraw
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  proposalId: 1
}
