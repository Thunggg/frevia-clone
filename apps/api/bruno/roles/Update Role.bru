meta {
  name: Update Role
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/roles/4
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Super Admin",
    "description": "Updated custom role description"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
