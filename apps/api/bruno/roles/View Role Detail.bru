meta {
  name: View Role Detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/roles/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
