meta {
  name: test-next
  type: http
  seq: 1
}

post {
  url: {{nextjsUrl}}/auth
  body: json
  auth: inherit
}

body:json {
  {
    "accessToken": "123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
